resources:
  default:
    routes:
      auto: true
  expose:
    entity: ~
    entity2:
      - get: /truc/:param
        handler: handler-example.simpleResolve
        security: security.rule1
      - post: /truc/:param
        handler: get.entities
        security: security.rule1
      - get: /truc2/:param
        handler: handler-example.modelResolve
        security: security.rule1
queue:
  listen:
    queueName: hook.service_name.function
